{
  "compilerOptions": {
    // Куда производится сборка, если сборка производится вебпаком - не важна
    "outDir": "./build/",
    // Подсвечивает места, где не указан тип (неявный any), заставляя разработчика писать any явно
    "noImplicitAny": true,
    // Способы модульной сборки, обычно es6 или ESNext, но на бекенде мб commonJS
    "module": "ESNext",
    // Версия ecma script в который код по итогу будет компилироваться, обычно ставят 5 или 6 версию, чтобы поддерживало большинство браузеров
    "target": "es5",
    "jsx": "react-jsx",
    // Компилятор будет обрабатывать не только TS файлы, но и JS
    "allowJs": true,
    "moduleResolution": "node",
    // esModuleInterop позволяет работать с пакетами, которые используют commonJs как с обычными, с помощью import:
    // before: import Moment from 'moment'; - Moment undefined
    // after: import Moment from 'moment'; - Moment is 'object'
    "esModuleInterop": true,
    // Если библиотека не имеет default import, вместо import * as React from 'react' можно писать import React from 'react'
    "allowSyntheticDefaultImports": true,
    "baseUrl": ".",
    "paths": {
      "*": ["src/*"]
    }
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS",
    },
  },
//  "include": [
//    "./config/jest/setupTests.ts",
//    "src/app/types/global.d.ts"
//  ]
}
